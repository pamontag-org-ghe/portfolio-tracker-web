# --- Backend ---
PORT=4000
NODE_ENV=development

# JWT signing secret (generate a long random string for production!)
JWT_SECRET=change-me-in-production-please

# Data storage: "local" (JSON files under backend/.data) or "cosmos"
STORAGE_DRIVER=local

# Cosmos DB (only required when STORAGE_DRIVER=cosmos)
# Leave COSMOS_KEY empty in production — the backend authenticates via
# DefaultAzureCredential (App Service managed identity). The key is only used
# for local development with the Cosmos emulator.
COSMOS_ENDPOINT=
COSMOS_KEY=
COSMOS_DATABASE=portfolio-tracker

# CORS - comma separated list of allowed origins
CORS_ORIGINS=http://localhost:5173

# --- Frontend ---
VITE_API_BASE_URL=http://localhost:4000/api
